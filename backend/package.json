{
  "name": "stellerpool-anchor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stellerpool'un kendi işlettiği minimal SEP-1/10/24 test anchor'ı (TRYT — TRY'yi temsil eden test varlığı; gerçek banka rayı değildir).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "setup-issuer": "tsx scripts/setup-issuer.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^17.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
